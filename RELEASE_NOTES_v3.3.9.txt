Kalshi Trading Backend v3.3.9 — Forward Pipeline Tracer

- Adds a non-writing background trace for the v3.3 forward pipeline.
- Trace stages: history checkpoint, date normalization, Kalshi markets, MLB research pipeline,
  recommendation building, frozen v3.1 scoring, and durable-ledger summary read.
- Each stage records elapsed time and counts, and emits the same markers to Render logs.
- Trace start returns immediately, avoiding the ~26 second gateway failure seen in the synchronous dry run.
- Trace can never append to the forward-validation ledger.
- Frozen v3.1 model architecture, Apr-Jul training history, >=10pt primary hypothesis, and 100-trade target are unchanged.
