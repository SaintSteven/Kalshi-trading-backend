Kalshi Trading Backend v1.0 — Historical Backtesting

Upload to the ROOT of GitHub:

Replace:
- main.py

Add:
- historical_backtest_collector.py
- historical_backtest_models.py
- example_historical_backtest_request.json

Leave all existing v0.9.1 files unchanged.

After deployment:
1. /health should show 1.0.0
2. /docs should show POST /historical-backtest
3. Test with example_historical_backtest_request.json

The backtest uses only information available before each game. A run is limited to 14 days by default to reduce timeout risk. This remains paper-only.
