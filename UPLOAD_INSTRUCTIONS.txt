Kalshi Trading Backend v1.1.1 — Import Fix

Replace only:
- lineup_experiment.py

The failed deployment occurred because lineup_experiment.py imported long function names that do not exist in the current historical_backtest_collector.py. The repository uses short names such as schedule, boxscore, actual_starters, fetch, person, and game_log.

No other files need to change.

After upload:
1. Render should redeploy automatically.
2. /health should show version 1.1.0 because main.py does not need to change.
3. /docs should show POST /lineup-experiment.
