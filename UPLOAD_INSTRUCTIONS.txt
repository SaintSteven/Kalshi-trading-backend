Kalshi Trading Backend v1.1 — Lineup Experiment

Upload ALL FOUR files to the ROOT of GitHub:
- main.py (replace the existing main.py)
- lineup_experiment.py
- lineup_experiment_models.py
- example_lineup_experiment_request.json

Do not use main_patch.txt. No manual code editing is needed.

After deployment:
1. /health should show version 1.1.0
2. /docs should show POST /lineup-experiment
3. Test one day first using example_lineup_experiment_request.json

Interpretation:
- mae_change below 0 = lineup model improved MAE
- rmse_change below 0 = lineup model improved RMSE
- improved true = lineup model beat the team-K% baseline

This remains paper-only.
